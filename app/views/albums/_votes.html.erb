<p>
  <strong>Votes:</strong>
  <%= @album.votes.size %>
  <% if user_signed_in? %>
    <% if vote = @album.votes.find_by_user_id(current_user.id) %>
      <%= link_to 'Un-vote!', album_vote_path(@album, vote), method: :delete, remote: true %>
    <% else %>
      <%= link_to 'Vote!', album_votes_path(@album), method: :post, remote: true %>
    <% end %>
  <% end %>
</p>
